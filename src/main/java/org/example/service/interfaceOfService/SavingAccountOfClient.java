package org.example.service.interfaceOfService;

import org.example.entity.Bank;
import org.example.entity.Client;

public interface SavingAccountOfClient {
    public void saveAccount(Client client, Bank bank);
}
